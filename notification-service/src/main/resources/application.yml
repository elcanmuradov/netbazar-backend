spring:
  application:
    name: notification-service
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          min-idle: 1
  mail:
    host: smtp.titan.email
    port: 465
    username: info@netbazar.tech
    password: 0506379198Ee!
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          connectiontimeout: ${SPRING_MAIL_SMTP_CONNECTION_TIMEOUT:5000}
          timeout: ${SPRING_MAIL_SMTP_TIMEOUT:5000}
          writetimeout: ${SPRING_MAIL_SMTP_WRITE_TIMEOUT:5000}

aws:
  enabled: false

mail:
  enabled: ${MAIL_ENABLED:true}

management:
  health:
    mail:
      enabled: false


server:
  port: 8080
